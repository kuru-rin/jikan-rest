apiVersion: v1
kind: Service
metadata:
  name: {{ include "jikan-rest.fullname" . }}
  labels:
    {{- include "jikan-rest.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jikan-rest.selectorLabels" . | nindent 4 }}
